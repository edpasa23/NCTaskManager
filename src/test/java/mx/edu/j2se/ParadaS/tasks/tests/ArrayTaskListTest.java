package mx.edu.j2se.ParadaS.tasks.tests;

import mx.edu.j2se.ParadaS.tasks.ArrayTaskList;
import mx.edu.j2se.ParadaS.tasks.Task;
import org.junit.Assert;
import org.junit.jupiter.api.Test;

import java.util.Iterator;

public class ArrayTaskListTest {

    @Test
    public void ArrayTaskListTest() throws Exception {
/*
        //Tasks
        Task task1 = new Task("Task1", 1, 10, 1);
        Task task2 = new Task("Task2", 2, 12, 2);
        Task task3 = new Task("Task3", 7);
        Task task4 = new Task("Task4", 9);

        //Array
        ArrayTaskList arrayT = new ArrayTaskList();

        arrayT.add(task1);
        arrayT.add(task2);
        arrayT.add(task3);
        arrayT.add(task4);

        task1.setActive(true);
        task2.setActive(true);
        task3.setActive(true);
        task4.setActive(true);

        Assert.assertEquals(4, arrayT.size());

        arrayT.remove(task2);

        Assert.assertEquals("Task3", arrayT.getTask(1).getTitle());

        ArrayTaskList test = arrayT.incoming(0, 15);

        Assert.assertEquals(3, test.size());

        for (Task x : arrayT) {
            System.out.println("Tarea:" + x.getTitle());
        }

        Iterator<Task> iteratorArrayTask = arrayT.iterator();
        while (iteratorArrayTask.hasNext()) {

            Task tmp = iteratorArrayTask.next();

            System.out.println("Tarea:" + tmp.getTitle());

        }

        String prueba = arrayT.toString();
        System.out.println(prueba);

        ArrayTaskList cloneTaskList = (ArrayTaskList) arrayT.clone();

        System.out.println(arrayT.hashCode());
        System.out.println(cloneTaskList.hashCode());
        System.out.println(arrayT.equals(cloneTaskList));

        cloneTaskList.remove(task3);

        System.out.println(prueba);
        String prueba2 = cloneTaskList.toString();
        System.out.println(prueba2);

        System.out.println(arrayT.hashCode());
        System.out.println(cloneTaskList.hashCode());


    }
*/
    }

}
